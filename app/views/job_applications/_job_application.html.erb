<div class="box">
	<%= link_to 'View', job_application %>
	<p class="title">
		<%= job_application.user_id %>
	</p>
	<p class="description">
		<%= job_application.job_posting_id %>
		<%= job_application %>
	</p>

  <% if current_user.id == job_application.user_id %>

  <p><%= link_to 'Edit', edit_job_application_path(job_application) %>
  <%= link_to 'Destroy', job_application, method: :delete, data: { confirm: 'Are you sure?' } %>
	</p>
  <% end %>
</div>
